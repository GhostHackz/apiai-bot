{
  "name": "Hello World!",
  "description": "Sample AI Webhook Demo!",
  "version": "0.0.1",
  "private": true,
  "license": "Apache Version 2.0",
  "author": "GhostHackz",
  "engines": {
    "node": "~6.0"
  },
  "scripts": {
    "lint": "semistandard --fix \"**/*.js\"",
    "start": "functions deploy aiManagementSystem --trigger-http",
    "deploy": "gcloud beta functions deploy aiManagementSystem --trigger-http --stage-bucket factsAboutGoogle"
  },
  "dependencies": {
    "actions-on-google": "^1.0.0"
  },
  "devDependencies": {
    "semistandard": "^9.1.0"
  }
}
